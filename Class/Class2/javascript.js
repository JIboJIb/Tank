let textform = (prompt())

class marker {
    constructor(color, quantity) {
        this.color = color
        this.quantity = quantity
    }
    write() {
        if (textform.length <= 200) {

        }
    }
}

let yellowmarker = new marker

document.write(textform)
console.log(textform.length)